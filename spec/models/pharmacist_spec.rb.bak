require 'rails_helper'

require 'spec_helper'

describe Pharmacist do

	it { expect(pharmacist).to validate_presence_of(:first_name) }

	it { expect(pharmacist).to validate_presence_of(:last_name) }

	it { expect(pharmacist).to have_many(:orders) }

end